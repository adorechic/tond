%h1
  Create New Room

= form_for @room do |f|
  - if @room.errors.any?
    %ul
      - @room.errors.full_messages.each do |msg|
        %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Create'
